<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawerLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:context=".ClaimRequestActivity">

    <LinearLayout
        android:id="@+id/contentArea"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <!-- toolbar -->
        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="#89CFF0"
            app:title="Submit a Claim"
            app:titleTextColor="#FFFFFF"
            app:popupTheme="@style/ThemeOverlay.AppCompat.Light"/>

        <!-- scroll -->
        <ScrollView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:fillViewport="true">

            <LinearLayout
                android:id="@+id/main"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="16dp">

                <!-- new claim -->
                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="New Claim Request"
                    android:textSize="18sp"
                    android:textStyle="bold"
                    android:textColor="#000000"
                    android:layout_marginBottom="12dp"/>

                <!-- claim type label -->
                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="Claim Type"
                    android:textSize="13sp"
                    android:textColor="#555555"
                    android:layout_marginBottom="4dp"/>

                <!-- claim type dropdown -->
                <AutoCompleteTextView
                    android:id="@+id/actvClaimType"
                    android:layout_width="match_parent"
                    android:layout_height="48dp"
                    android:hint="Select claim type"
                    android:padding="10dp"
                    android:focusable="true"
                    android:focusableInTouchMode="true"
                    android:clickable="true"
                    android:background="@android:drawable/edit_text"
                    android:layout_marginBottom="14dp"/>

                <!-- description label -->
                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="Description"
                    android:textSize="13sp"
                    android:textColor="#555555"
                    android:layout_marginBottom="4dp"/>

                <!-- description edit -->
                <EditText
                    android:id="@+id/etDescription"
                    android:layout_width="match_parent"
                    android:layout_height="120dp"
                    android:hint="Describe your request"
                    android:inputType="textMultiLine"
                    android:gravity="top|start"
                    android:padding="10dp"
                    android:background="@android:drawable/edit_text"
                    android:layout_marginBottom="18dp"/>

                <!-- submit button -->
                <Button
                    android:id="@+id/btnSubmitClaim"
                    android:layout_width="match_parent"
                    android:layout_height="50dp"
                    android:text="Submit Claim"
                    android:textColor="#FFFFFF"
                    android:backgroundTint="#89CFF0"
                    android:layout_marginBottom="8dp"/>

                <!-- result message -->
                <TextView
                    android:id="@+id/tvClaimResult"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text=""
                    android:textSize="13sp"
                    android:textColor="#0000FF"
                    android:gravity="center"
                    android:layout_marginBottom="20dp"/>

                <!-- divider -->
                <View
                    android:layout_width="match_parent"
                    android:layout_height="1dp"
                    android:background="#FFFFFF"
                    android:layout_marginBottom="16dp"/>

                <!-- header myclaims -->
                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    android:gravity="center_vertical"
                    android:layout_marginBottom="10dp">

                    <TextView
                        android:layout_width="0dp"
                        android:layout_height="wrap_content"
                        android:layout_weight="1"
                        android:text="My Claims"
                        android:textSize="18sp"
                        android:textStyle="bold"
                        android:textColor="#000000"/>

                    <Button
                        android:id="@+id/btnRefreshMyClaims"
                        android:layout_width="wrap_content"
                        android:layout_height="36dp"
                        android:text="↻Refresh"
                        android:textColor="#FFFFFF"
                        android:backgroundTint="#89CFF0"
                        android:paddingStart="12dp"
                        android:paddingEnd="12dp"
                        android:textSize="12sp"/>

                    <Button
                        android:id="@+id/btnDeleteMyClaim"
                        android:layout_width="wrap_content"
                        android:layout_height="36dp"
                        android:text="Delete Selected Claim"
                        android:textColor="#FFFFFF"
                        android:backgroundTint="#89CFF0"
                        android:paddingStart="12dp"
                        android:paddingEnd="12dp"
                        android:textSize="12sp"/>
                </LinearLayout>

                <!-- filter my claims by status -->
                <AutoCompleteTextView
                    android:id="@+id/actvMyClaimsFilter"
                    android:layout_width="match_parent"
                    android:layout_height="44dp"
                    android:hint="Filter my claims by status"
                    android:padding="10dp"
                    android:focusable="false"
                    android:focusableInTouchMode="false"
                    android:background="@android:drawable/edit_text"
                    android:layout_marginBottom="10dp"/>

                <!-- my claims list -->
                <ListView
                    android:id="@+id/lvMyClaims"
                    android:layout_width="match_parent"
                    android:layout_height="220dp"
                    android:divider="#FFFFFF"
                    android:dividerHeight="1dp"
                    android:background="#FFFFFF"
                    android:layout_marginBottom="14dp"/>

                <!-- selected claim details -->
                <TextView
                    android:id="@+id/tvMyClaimDetail"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:minHeight="100dp"
                    android:text="Tap a claim to view details"
                    android:padding="14dp"
                    android:gravity="top|start"
                    android:background="#FFFFFF"
                    android:textColor="#555555"
                    android:lineSpacingMultiplier="1.4"
                    android:layout_marginBottom="16dp"/>

            </LinearLayout>
        </ScrollView>
    </LinearLayout>

    <!-- sidebar -->
    <com.google.android.material.navigation.NavigationView
        android:id="@+id/navView"
        android:layout_width="280dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:fitsSystemWindows="true"
        app:headerLayout="@layout/nav_header"
        app:menu="@menu/nav_menu"/>

</androidx.drawerlayout.widget.DrawerLayout>